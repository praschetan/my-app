Push the latest changes to GitHub.

Steps:
1. Run `git status` to show what's changed
2. If nothing to commit, say so and stop
3. Run `git add .`
4. Look at the diff and suggest a commit message based on what actually changed, ask me to confirm or modify it
5. Commit with the approved message
6. push to: `personal` (https://github.com/praschetan/my-app.git)
7. Push to the chosen remote on branch main
8. Confirm success and show the commit hash